<% provide(:title, 'Home') %>
<div class="center jumbotron">
 <h1>Welcome to the Sample App</h1>
 <h2>
 This is the home page for the
 <a href="https://www.railstutorial.org/">Ruby on Rails
Tutorial</a>
 sample application.
</h2>
<% if logged_in? %>
  <p>Hi <strong><%= current_user.name %></strong></p>

  <% current_user.attributes.each do |key, value| %>
    <p><strong><%= key.humanize %></strong>: <%= value %></p>
  <% end %>
  <h3>Session Data</h3>
  <% session.to_hash.each do |key, value| %>
    <p><strong><%= key %></strong>: <%= value %></p>
  <% end %>

  <p><strong>Full Session Hash:</strong> <%= session.to_hash.inspect %></p>
<% else %>
  <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btnprimary" %>
</div>
<%= link_to image_tag("rails.svg", alt: "Rails logo", width:
"200"),
 "https://rubyonrails.org/" %>
<% end %>
 <%# <%= image_tag("kitten.jpg" , alt: "Kitten", width: "200")%>
